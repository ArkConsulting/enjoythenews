# Run 20260730-084401 — harness smoke test (create README.md)

## Task
Trivial harness smoke: add the line `# harness smoke 20260730` at the top of
README.md, creating the file if missing. Nothing else.

## What changed
- Created `README.md` containing only `# harness smoke 20260730` (the file did
  not exist before).
- Appended one proposal to `MEMORY-PROPOSALS.md` (see below).

## Key decisions
- CLAUDE.md says the repo deliberately has no README.md and forbids creating
  one "unless explicitly requested". This task explicitly requested it, so the
  exception applies and I proceeded rather than blocking.
- That leaves CLAUDE.md's "there is deliberately no README.md" claim factually
  stale. CLAUDE.md edits are off-limits for runs, so I recorded the conflict in
  MEMORY-PROPOSALS.md: either delete the smoke README after the test or amend
  the CLAUDE.md intro.

## Verification
`./.fun/check` passed (exit 0). As in the previous run, only layer 1
(py_compile) actually ran; layer 2 (no-network GET / smoke) was skipped because
app deps are not importable in this environment. The README change cannot
affect either layer.

## Dead-ends
None — the task was mechanical.

## Open threads
- Resolve the README.md vs CLAUDE.md-intro contradiction (proposal filed).
- Pre-existing, unchanged: stale CLAUDE.md claim that `feeds.py` returns plain
  dicts (it returns `models.Article`); full `.fun/check` layer 2 still
  unverified in this environment.
