# Run 20260805-101117 — graduate pending MEMORY-PROPOSALS.md rewordings into CLAUDE.md

## Task
Graduate the pending proposals in MEMORY-PROPOSALS.md into durable memory
(CLAUDE.md editing explicitly allowed for this graduation run).

## Count discrepancy (flagged, not blocking)
The task said "3 pending proposals" but MEMORY-PROPOSALS.md contained **2**
(one dated section, 20260805-095943, with two numbered proposals) — STATE.md
agrees it held exactly those two. I processed the two that exist; nothing was
skipped. If a third proposal was expected, it never landed in the file.

## What changed
Both proposals were CLAUDE.md rewordings from run 20260805-095943; both are
always-on guidance and both were verified against the code before applying
(case 1: GRADUATE into CLAUDE.md, date-stamped "(rule added 2026-08-05)"):

1. **Frontend pattern** — replaced the false-as-description bullet
   "Backend serves JSON endpoints. Frontend fetches and renders. No HTML
   partials over the wire." with the proposed guideline: full pages are
   server-rendered Jinja2; *new* dynamic endpoints return JSON for the page's
   JS; never HTML partials/fragments over the wire.
2. **Known constraints** — replaced the stale "No background scheduler"
   bullet with the current truth: hourly in-process asyncio refresh task in
   `main.py` (`REFRESH_INTERVAL`), plus startup and manual `/refresh`; no
   external scheduler; the task dies with the process.

Verification of the claims: `main.py` has `REFRESH_INTERVAL = 60 * 60`
(line 62), `_periodic_refresh()` started at startup (line 70) via
`asyncio.to_thread(_do_refresh)`, cancelled on shutdown — matches proposal 2
exactly. Proposal 1's premise (index server-rendered, `/refresh` returns
HTML) was confirmed in the previous run's review.

Both graduated bullets were deleted from MEMORY-PROPOSALS.md; the file is now
just its header again (as after the 2026-07-15 graduation).

## Key decisions
- Applied the proposed wordings verbatim (reflowed to CLAUDE.md's one-line
  bullet style) — they were written for exactly these two spots and the
  previous run's reviewer/gate already LGTM'd their substance.
- No `→ patterns/` or leave-unchanged cases: both proposals were
  app-specific CLAUDE.md fixes, nothing cross-app.
- Left TODO.md untouched: its two related items are already `[x]` and
  describe the proposal step; the application is this run's record.

## Known dead-ends
None.

## Open threads
- CLAUDE.md and the code now agree on background scheduling — the
  contradiction flagged by the previous gate is resolved once this merges.
- The standing human decisions (Norwegian copy carve-out, `tools/editor.py`
  archive-or-keep, demo content intent) remain open in TODO.md.

`./.fun/check`: PASS (both layers) before commit.
