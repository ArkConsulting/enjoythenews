# Run 20260715-171639 — graduate the 2 pending memory proposals

## Task
Graduate the two pending bullets in MEMORY-PROPOSALS.md into durable memory
(CLAUDE.md, other standing docs, or leave tagged for patterns/), delete the
graduated bullets, verify with ./.fun/check, and commit.

## What changed
- **CLAUDE.md** (graduation run — edits allowed):
  - Intro line now states there is deliberately **no README.md** — CLAUDE.md is
    the repo's only top-level documentation, and future runs must not create a
    README unless explicitly asked. Graduates the "no README" proposal and
    closes the matching STATE.md open thread (stale CLAUDE.md docs about it).
  - The `feeds.py` line under Architecture → Request flow now describes the real
    fetch mechanism: bytes fetched via stdlib `urllib` with a bounded timeout
    (`FEED_TIMEOUT`), then parsed by `feedparser.parse()` — with the explicit
    warning to never pass `feedparser` a URL (it fetches with no timeout).
    Graduates the feedparser-timeout proposal.
- **MEMORY-PROPOSALS.md**: both bullets deleted; header kept. File is now empty
  of proposals.

## Key decisions
- Both proposals graduated into CLAUDE.md (case 1). The feedparser learning's
  full rationale (User-Agent trick, stdlib-first reasoning) already lives as
  code comments at `feeds.py:15-26`, so CLAUDE.md only carries the concise
  always-on constraint; the code comment remains the detailed reference.
- Did NOT tag the feedparser bullet '→ patterns/': no other workspace app uses
  RSS/feedparser, and patterns/ has no related pattern file. If a second app
  ever fetches feeds, this is a candidate to lift.
- Left CLAUDE.md's claim that `feeds.py` "returns plain dicts" untouched even
  though the code now returns `models.Article` objects — fixing it was not
  justified by any pending proposal (graduation runs must not make unrelated
  CLAUDE.md changes). Noted here as a known stale spot for a human or future
  proposal.

## Dead-ends
None.

## Open threads
- CLAUDE.md Architecture says `feeds.py` returns plain dicts; it actually
  returns `models.Article` instances (see `feeds.py:40-60`, `models.py`).
  Stale doc, out of scope for this run.
- ./.fun/check ran layer 1 only (py_compile); layer 2 (network-less smoke of
  GET /) skipped because app deps aren't importable in this environment. Exit 0.
