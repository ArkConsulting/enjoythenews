# Run 20260731-145043 — audit CLAUDE.md against the repo

## Task
Audit every rule/claim in CLAUDE.md against the repo as of today; fix or delete
STALE claims, merge REDUNDANT ones, leave and list UNVERIFIABLE ones. The
CLAUDE.md diff is the product of this run. MEMORY-PROPOSALS.md was empty, so
each edit is justified by a documented contradiction in the repo (listed below),
not by a pending proposal.

## STALE — fixed (each verified against code)
- **Architecture header** said "Three flat modules + Jinja2 templates. No
  subdirectory nesting." Reality: five flat modules (`main`, `feeds`, `db`,
  `models`, `ai`) plus `agent/` and `tools/` packages. Rewritten.
- **Startup/refresh flow** said `feeds.fetch_all()` → `db.upsert_articles()`.
  Reality (`main.py:152-158`): fetch → `db.filter_new()` → `ai.classify()` →
  upsert. Fixed; added short entries for `ai.py` and `models.py` and noted the
  `/edit` routes proxy to the `agent/` service on :8766.
- **feeds.py "returns plain dicts"** — returns `models.Article` dataclass
  instances (`feeds.py:40-60`). This was the open thread in STATE.md; now fixed.
- **Frontend "Tailwind via CDN"** (both in Frontend pattern and Known
  constraints) — `src/index.html` and `src/edit.html` use hand-written CSS in a
  `<style>` block + Google Fonts; no Tailwind anywhere in `src/`. Replaced the
  pattern bullet; deleted the Known-constraints bullet.
- **Frontend "src/index.html — single self-contained file"** — `src/edit.html`
  exists too; both now named.
- **"Adding news sources"** referenced a `source_colors` dict and "filter key in
  the UI". Neither exists: colors are per-source CSS classes
  (`src/index.html:168-170`, `.source-<Name-with-dashes>`), and the UI has no
  source filter (`db.get_articles(source=...)` exists but no route uses it).
  Rewritten to match the CSS-class reality.
- **ops/ tree** listed `ops/ssl-setup.sh` — file does not exist. Line removed.
- **tools/ tree** listed `generate.py` and `preview.py` — neither exists;
  `tools/editor.py` does. Listing now shows the real files (`version.py`,
  `editor.py`).
- **Agent loop (#3, top section)** claimed present-tense Ollama/LiteLLM routing;
  no `litellm`/`ollama` anywhere in code or requirements — `agent/claude.py` is
  Anthropic-only. Reworded: model-agnostic routing is *planned*, current backend
  is Anthropic-only. Intent preserved.
- **designs/ "Tailwind CDN"** — 4 of 5 variants use Tailwind; `stripe` is plain
  CSS. Softened to "Tailwind CDN or plain CSS".

## REDUNDANT — merged
- "What we are building" opened with "two purposes" and duplicated the
  enjoythenews + Lovable descriptions from the top "Three things" section (and
  contradicted its count of three). Replaced the duplicate intro with a pointer
  to the top section; the four-component detail stays.

## UNVERIFIABLE / left for the human
- Deployment claims: "deployed on Hetzner", "real users, real traffic",
  "Production always runs HEAD of main", the deploy-flow diagram. Outside the
  repo; left untouched.
- **Language rule vs. Norwegian UI copy**: the rule says templates must be
  English, but `src/index.html` and `main.py` (`_timeago`) contain Norwegian
  user-facing copy ("Akkurat nå", "I går", hardcoded Norwegian article text) —
  presumably intentional product language ("Horisonten"). The rule may need a
  carve-out for user-facing copy; not touched (convention rule, human call).
- **`tools/editor.py` appears dead**: nothing imports it; `main.py` duplicates
  its `EDITOR_SYSTEM` prompt and streams via `agent/` instead. Kept in the
  tools/ listing since the file exists, but archiving it (per the archive/
  rule) is a human decision.
- `src/index.html` contains hardcoded Norwegian demo content in extra tab
  sections (e.g. lines ~460-465) alongside the Jinja2 article loop — unclear if
  intentional; not a CLAUDE.md matter, just noting it.
- Safety/gate rules (External actions, Deleting files / archive/, push
  confirmation) — verified consistent with repo (`archive/` is gitignored),
  deliberately untouched per task.
- "Backend serves JSON endpoints. Frontend fetches and renders" — today the
  index page is fully server-rendered and `/refresh` returns full HTML; the
  `/edit` endpoints do serve JSON/SSE. Read it as a guideline for dynamic
  features rather than a description, so left as-is; flagging in case it should
  be reworded.

## Key decisions
- Treated the audit task itself as the justification framework (documented
  contradictions above), since MEMORY-PROPOSALS.md was empty — the task
  explicitly makes the CLAUDE.md diff the product of this run.
- Kept every vision/strategy statement (versioning, dogfooding, model sizing)
  untouched; ai.py's Haiku classification matches the model-sizing rule.
- No git tags exist yet (`git tag` is empty) — consistent with the versioning
  strategy ("unreleased" handled in `tools/version.py`), so not a finding.

## Dead-ends
None.

## Check
`./.fun/check` passes (layer 1; layer 2 skipped — app deps not importable in
the container, same as previous runs).
