# Memory proposals

Durable learnings appended by unattended runs. Review and fold into CLAUDE.md /
patterns as appropriate.

- 2026-07-30: A README.md now exists (explicitly requested by a harness smoke
  run; contains only `# harness smoke 20260730`). CLAUDE.md's intro still states
  "there is deliberately no README.md" — either remove the smoke README after
  the test, or soften that CLAUDE.md claim. Decide one way; don't leave both.
